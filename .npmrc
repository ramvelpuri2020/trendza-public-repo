# The real app ships a mix of @capacitor/* major versions (a historical
# artifact of incremental upgrades). Peer-dep resolution is strict by default
# in npm ≥7, which rejects that mix even though the installed packages work.
# `legacy-peer-deps` lets `npm install` succeed out of the box.
legacy-peer-deps=true